# نشر النسخة العربية من الهاتف

1. احتفظ بنسخة من المستودع الحالي قبل الاستبدال.
2. ارفع **محتويات** المجلد `devahmad` إلى جذر مستودع GitHub.
3. احذف مجلد `src/pages/[lang]` القديم من GitHub إن كان ما يزال موجودًا، واحذف ملفات الواجهة القديمة التي استُبدلت.
4. تأكد أن `src/pages/index.astro` هو الملف الجديد وأن `astro.config.mjs` محدث.
5. اضغط Commit changes وانتظر نجاح نشر Cloudflare.
6. افتح `/` ثم `/about/` ثم `/blog/` للتحقق.
7. بعد التأكد، يمكن إنشاء تحويلات 301 من روابط `/ar/` القديمة إلى الروابط الجديدة عند الحاجة.

Cloudflare: Build command `npm run build`؛ Output `dist`.
